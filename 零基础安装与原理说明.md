# OnlySight（唯见）防沉迷自律 App — 零基础安装、原理与制作流程说明

> 本文档面向 **零 Android 开发基础** 的大学生。  
> 假设你已经（或即将）拥有完整的 OnlySight（唯见）工程文件夹（包含 `app/`、`build.gradle` 等文件）。

---

## 目录

1. [这个 App 是干什么的？](#1-这个-app-是干什么的)
2. [App 的工作原理（用人话讲）](#2-app-的工作原理用人话讲)
3. [App 是怎么做出来的（制作流程）](#3-app-是怎么做出来的制作流程)
4. [装到手机上的完整步骤](#4-装到手机上的完整步骤)
5. [第一次使用：必做的设置](#5-第一次使用必做的设置)
6. [常见问题与排查](#6-常见问题与排查)
7. [名词小词典](#7-名词小词典)

---

## 1. 这个 App 是干什么的？

**OnlySight（唯见）** 是一款帮你 **自律、防沉迷** 的 Android 应用，核心理念是：**极简、本地运行、自己带 API Key（BYOK）**。

| 功能 | 说明 |
|------|------|
| **AI 监督** | 后台读取屏幕上的文字，发给大语言模型判断你在「学习/工作」还是「刷娱乐视频」 |
| **强制打断** | 若判定为娱乐，自动退回桌面，并记录一次「被打断」 |
| **番茄钟** | 25 分钟专注计时，一键开始/暂停 |
| **统计** | 今日被打断次数、专注总时长 |
| **API 配置** | 填写兼容 OpenAI 格式的接口地址、Key、模型名 |
| **关于** | 支持开发者、Bug 反馈、中/英语言切换 |

> **重要**：这不是游戏加速器，也不是系统级家长控制。它依赖 **无障碍服务** 读取屏幕文字，需要你 **手动授权**，且需要 **自己准备 API Key**（例如 OpenAI 或国内兼容接口）。

---

## 2. App 的工作原理（用人话讲）

可以把它想成一条流水线：

```
你在看手机
    ↓
无障碍服务「看到」屏幕上的文字（不截图，读文字节点）
    ↓
屏幕内容稳定约 3 秒（防抖，避免频繁请求）
    ↓
把文字打包，通过 HTTPS 发给你的大模型 API
    ↓
模型只回复一个数字：0 = 正常，1 = 娱乐
    ↓
若是 1 → 执行「退回桌面」+ 记一次统计 + 弹出提示
```

### 2.1 为什么需要「无障碍服务」？

Android 出于隐私保护，**普通 App 不能直接读别的 App 屏幕内容**。  
**无障碍服务（Accessibility Service）** 是系统提供的官方能力，供读屏、辅助功能类 App 使用。

OnlySight（唯见）会：

1. 深度优先遍历当前界面上的 **可见文字**（例如视频标题、评论、按钮文字）；
2. **不会** 随意上传整张截图（本设计以文本为主，更省流量、更隐私）；
3. 只有你在系统设置里 **明确开启** 后才会工作。

### 2.2 「防抖 3 秒」是什么意思？

你滑动、切换页面时，屏幕文字会变来变去。若每一帧都请求 API，会：

- 浪费钱（API 按次/按 token 计费）；
- 拖慢手机；
- 误判增多。

所以 App 会等屏幕内容 **停止剧烈变化约 3 秒** 后，才打包发送一次。

### 2.3 大模型在这里做什么？

App 会发送类似这样的请求（OpenAI `chat/completions` 格式）：

- **系统提示词（中文）**：  
  「你是一个严格的自律监督员。请分析屏幕文本，判断用户是在学习工作还是在看娱乐视频消磨时间。娱乐回复 1，正常使用回复 0，仅回复数字。」

- **用户内容**：当前屏幕抓取到的文字摘要。

模型 ideally 只回 `0` 或 `1`，App 据此决定是否打断。

### 2.4 打断时具体做了什么？

当 API 返回 **1**（娱乐）时：

1. 调用 `performGlobalAction(GLOBAL_ACTION_HOME)` → **退回桌面**（类似按 Home 键）；
2. 在本地数据库记 **一次打断**；
3. 显示 **Toast 或通知**，提醒你被 AI 打断了。

### 2.5 其他模块在干什么？

| 模块 | 原理 |
|------|------|
| **番茄钟** | 本地计时器（正计时/倒计时），状态存在手机本地 |
| **统计** | Room 数据库或 DataStore 存每日打断次数、专注时长 |
| **API 配置** | DataStore 加密或普通本地存储 Key（不上传服务器） |
| **多语言** | `values/strings.xml`（中文）、`values-en/strings.xml`（英文），随系统或手动切换 |

### 2.6 架构一图流

```
┌─────────────────────────────────────────┐
│           界面层 (Jetpack Compose)       │
│   首页 │ 统计 │ API 配置 │ 关于          │
└─────────────────┬───────────────────────┘
                  │ 观察/操作
┌─────────────────▼───────────────────────┐
│         ViewModel (MVVM)                 │
│   管理 UI 状态、调用仓库、协程异步        │
└─────────────────┬───────────────────────┘
                  │
    ┌─────────────┼─────────────┐
    ▼             ▼             ▼
 DataStore      Room DB     Retrofit/OkHttp
 (配置/语言)   (统计数据)    (调用 LLM API)
                  ▲
                  │
┌─────────────────┴───────────────────────┐
│   FocusAccessibilityService (后台)       │
│   抓屏文字 → 防抖 → 调 API → 打断        │
└─────────────────────────────────────────┘
```

---

## 3. App 是怎么做出来的（制作流程）

下面是从零到一的大致 **开发顺序**，方便你理解「代码从哪来」。

### 阶段 1：搭工程骨架

1. 用 **Android Studio** 新建 **Empty Activity** 项目，语言选 **Kotlin**，UI 选 **Jetpack Compose**；
2. 配置 `minSdk`（建议 26+）、依赖：Compose、Navigation、Room、DataStore、Retrofit、OkHttp、协程等；
3. 规划包结构，例如：

```
com.focusai.app/
├── ui/              # 四个 Tab 的界面
├── viewmodel/       # MVVM 的 ViewModel
├── data/            # 数据库、DataStore、API 接口
├── service/         # AccessibilityService
├── util/            # 工具类
└── MainActivity.kt
```

### 阶段 2：做界面（Compose + 底部导航）

1. 底部四个 Tab：**Focus / Stats / Settings / About**；
2. 中英文字符串放进 `res/values/strings.xml` 和 `res/values-en/strings.xml`；
3. ViewModel 暴露 `StateFlow`，界面用 `collectAsState()` 显示。

### 阶段 3：本地存储

1. **DataStore**：存 API Base URL、Key、Model、语言偏好；
2. **Room**：存「打断记录」「专注时长」等，供统计页查询。

### 阶段 4：无障碍服务（核心）

1. 新建类继承 `AccessibilityService`；
2. 在 `AndroidManifest.xml` 注册，并写 `accessibility_service_config.xml`；
3. 在 `onAccessibilityEvent` 里收集文字，用协程 + 防抖逻辑调 API；
4. 根据结果执行 Home、写库、发通知。

### 阶段 5：网络层

1. 用 Retrofit 定义 `POST /v1/chat/completions`；
2. 请求体符合 OpenAI 格式：`model`、`messages`（system + user）；
3. 解析回复中的 `choices[0].message.content`，取 `0` 或 `1`。

### 阶段 6：联调与真机测试

1. 手机打开 **USB 调试**；
2. Android Studio 点 **Run**，装到手机；
3. 手动开无障碍、填 API、刷视频测试是否打断。

### 阶段 7：打包发布（可选）

- **Debug 包**：自己用，签名简单；
- **Release 包**：需 keystore 签名，才能长期安装或上架（本项目开源本地用，Debug 即可）。

---

## 4. 装到手机上的完整步骤

你需要：**一台 Windows/Mac 电脑** + **一部 Android 手机** + **数据线**（或同一 WiFi 无线调试）。

### 4.1 电脑端：安装 Android Studio

1. 打开浏览器，搜索 **Android Studio 官网**：  
   https://developer.android.com/studio  
2. 下载并安装（一路「Next」，磁盘空间建议预留 **10GB+**）；
3. 首次启动时，按向导安装 **Android SDK**（选默认即可）。

> 安装较慢是正常的，首次可能要 20～40 分钟，取决于网络。

### 4.2 打开 OnlySight（唯见）工程

1. 把完整工程文件夹放到电脑（例如 `D:\桌面\focusai`）；
2. 打开 Android Studio → **Open** → 选中该文件夹；
3. 等待底部 **Gradle Sync** 完成（第一次会下载依赖，需联网）。

若提示缺少 SDK，点 **Install** 按提示安装即可。

### 4.3 手机端：打开开发者选项与 USB 调试

不同品牌略有差异，通用步骤：

1. 打开 **设置 → 关于手机**；
2. 连续点击 **版本号** 7 次，提示「您已处于开发者模式」；
3. 返回 **设置 → 系统 → 开发者选项**（或「更多设置」里）；
4. 打开 **USB 调试**；
5. 用数据线连接电脑，手机弹出「是否允许 USB 调试」→ 选 **允许**（可勾选始终允许）。

#### 小米 / 红米 / 华为等额外注意

- 部分机型需同时打开 **USB 安装** 或 **仅充电模式下允许 ADB**；
- 华为可能需安装 **HiSuite 驱动** 或换原装线；
- 若始终认不出设备，在 Android Studio 顶部设备栏点下拉 → **Troubleshoot Device Connections**。

### 4.4 方法一：直接 Run 安装（推荐新手）

1. Android Studio 顶部工具栏，设备栏选中你的 **手机型号**；
2. 点击绿色 **▶ Run**（或按 `Shift + F10`）；
3. 等待编译完成，手机会自动安装并打开 **OnlySight（唯见）**。

看到 App 图标出现在桌面即成功。

### 4.5 方法二：生成 APK 文件再拷贝（无法连电脑时）

1. 菜单 **Build → Build Bundle(s) / APK(s) → Build APK(s)**；
2. 完成后点通知里的 **locate**，找到类似：  
   `app/build/outputs/apk/debug/app-debug.apk`；
3. 把 `app-debug.apk` 发到手机（微信、QQ、网盘、数据线复制均可）；
4. 手机上点击 APK 安装；若提示「未知来源」，到设置里 **允许安装未知应用**。

> Debug 包仅供自用测试；换电脑重新编译后可直接覆盖安装。

### 4.6 方法三：无线调试（Android 11+，可选）

1. 手机和电脑连 **同一 WiFi**；
2. 开发者选项里打开 **无线调试**，配对 Android Studio；
3. 之后可不插线 Run（具体以 Android Studio 当前版本向导为准）。

---

## 5. 第一次使用：必做的设置

安装成功 ≠ 能监督，还需以下步骤：

### 步骤 1：配置 API（「配置」Tab）

| 字段 | 示例 | 说明 |
|------|------|------|
| Base URL | `https://api.openai.com/v1` | 兼容 OpenAI 的接口地址 |
| API Key | `sk-...` | 在服务商官网申请，**勿泄露** |
| Model | `gpt-4o-mini` 等 | 填该接口支持的模型名 |

点 **保存**。

> 若用国内中转/兼容服务，Base URL 和 Model 以服务商文档为准。

### 步骤 2：开启无障碍权限（「首页」Tab）

1. 点 **去授权 / 打开无障碍设置**；
2. 在系统列表找到 **OnlySight（唯见）**（或你的应用名）；
3. 打开开关，确认权限说明；
4. 返回 App，确认显示 **AI 监督已开启**。

### 步骤 3：打开 AI 监督开关

在首页打开监督总开关（若设计为独立开关）。

### 步骤 4：试跑

1. 先打开文档或笔记类 App，正常使用 → 一般 **不应** 被打断；
2. 再打开短视频 App 刷一会儿 → 若 API 正常，稳定约 3 秒后可能被打退回桌面；
3. 到 **统计** Tab 查看打断次数是否增加。

### 步骤 5：语言（可选）

在 **关于** Tab 可选：跟随系统 / 强制中文 / 强制英文。

---

## 6. 常见问题与排查

### Q1：Android Studio 找不到手机

- 换数据线（需能传数据，不是仅充电线）；
- 重插 USB，点允许调试；
- Windows 可安装 **Google USB Driver**（SDK Manager → SDK Tools）；
- 命令行执行 `adb devices`，应看到 `device` 而非 `unauthorized`。

### Q2：Gradle Sync 失败

- 检查网络；校园网可能需要代理；
- 确认已安装 **JDK 17**（Android Studio 自带 JBR 一般即可）；
- 菜单 **File → Invalidate Caches → Invalidate and Restart**。

### Q3：装了 App 但 AI 从不打断

按顺序检查：

1. 无障碍是否真的 **开启**（系统设置里再看一眼）；
2. API Key、Base URL、Model 是否正确；
3. 手机能否上网（浏览器开网页测试）；
4. Logcat 里是否报网络 401/404（Key 错或 URL 错）；
5. 有些 App 界面文字少，模型可能难判断——属正常现象。

### Q4：打断太频繁 / 误判

- 娱乐判定依赖 **屏幕文字**，纯游戏画面可能文字少；
- 可换更强模型或调整 Prompt（需改代码，进阶内容）；
- 确保防抖 3 秒已生效，避免重复请求。

### Q5：API 要花钱吗？

- OpenAI 等按 token 计费，**你自己承担**；
- 本 App 设计为 **BYOK**：Key 只存在你手机本地，不经开发者服务器。

### Q6：会不会泄露隐私？

- 屏幕 **文字** 会发到你配置的 API 服务器；
- 务必使用 **可信** 的 API 提供商；
- 开源代码可自行审计，敏感场景请谨慎使用。

### Q7：应用商店能搜到吗？

- 本项目为 **开源 + 自编译安装**，不经过商店；
- 自装 APK 需允许「未知来源」，属正常情况。

---

## 7. 名词小词典

| 名词 | 解释 |
|------|------|
| **Kotlin** | Android 官方推荐的编程语言 |
| **Jetpack Compose** | 用代码写 UI 的现代框架 |
| **MVVM** | Model-View-ViewModel，界面与逻辑分离的架构 |
| **Gradle** | Android 的构建工具，负责编译、下载依赖 |
| **APK** | Android 安装包文件 |
| **ADB** | Android 调试桥，电脑与手机通信的工具 |
| **AccessibilityService** | 无障碍服务，读屏辅助的核心 |
| **Retrofit** | 发 HTTP 请求的网络库 |
| **Room** | 本地 SQLite 数据库封装 |
| **DataStore** | 存键值对配置的本地存储 |
| **BYOK** | Bring Your Own Key，自带 API 密钥 |
| **OpenAI 格式** | `POST /v1/chat/completions` + `messages` 数组的请求结构 |

---

## 附录：推荐学习顺序（想自己改代码时）

1. Kotlin 基础语法（变量、函数、类）— 1～2 周；
2. Android 四大组件概念（Activity、Service）— 3～5 天；
3. Jetpack Compose 入门 — 1 周；
4. 协程（异步网络请求）— 3～5 天；
5. 再读 OnlySight（唯见）源码：从 `MainActivity` → 各 Tab → `AccessibilityService`。

B 站搜索「Android Kotlin 入门」「Jetpack Compose 教程」即可，选近年的视频。

---

## 文档信息

- **适用项目**：OnlySight（唯见）防沉迷自律 App  
- **目标读者**：零基础大学生  
- **最后更新**：2026-05-26  

若工程文件夹里还没有完整代码，可在 Cursor 里对 AI 说：「请按之前的架构生成完整 Android 工程」，生成后再按 **第 4 节** 安装即可。

如有具体报错（Gradle 红字、Logcat 日志），把 **完整报错截图或文字** 发给协助你的人，排查会快很多。

祝自律顺利。
